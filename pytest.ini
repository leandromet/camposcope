[pytest]
testpaths = tests
markers =
    live: hits the real CAR GeoServer; excluded from the default run so CI never
          depends on a third party's uptime (doc/09-dev-environment.md §6)
addopts = -m "not live"
